import React from "react";
import { ReducerAction, ReducerState } from "../../state/Types";

export const QuizSession = ({
  state,
  dispatch,
}: {
  state: ReducerState;
  dispatch: React.Dispatch<ReducerAction>;
}) => {
  // TODO: state update for quiz session

  return <div>start</div>;
};
